spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/phoneBook
    username: test
    password: test

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true